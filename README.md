# 房產決策分析儀 (Property Decision Assistant) v7.0

這是一個使用 React + Vite + TypeScript 建置的個人化房產決策分析工具。

它協助使用者輸入、管理和比較多個待選房產物件，並根據**可自訂權重**（通勤、成本、空間、屋齡）為每個物件計算「綜合推薦分數」，幫助使用者在看房過程中做出更理性的決策。

## 🚀 核心功能 (V7.0)

* **[Bug 修正]** 修正了 V6.0 中，點擊「物件列表」頁籤會導致頁面崩潰的嚴重錯誤。
* **[儀表板升級] 釘選 (Pin) 到儀表板**：
    * 移除 V6.0 自動顯示 Top 5 的功能。
    * 物件卡片上新增「釘選 (Pin)」按鈕 (☆)。
    * 儀表板現在只會顯示您**主動釘選**的物件（最多 5 個），讓您完全控制要比較的目標。
* **[儀表板修復] 雷達圖**：
    * 修復了 V6.0 雷達圖，現在可正確顯示**多個物件**在「成本」、「通勤」、「空間」、「屋齡」四個維度上的分數。
* **[新工具頁] 專業分析 (V7.0)**：
    * 移除了 V6.0 的「檢查清單」和「術語」。
    * 新增 **"What-If" 財務情境分析**：模擬利率、頭期款變化對**所有物件**月付金的影響。
    * 新增 **房貸攤還試算**：包含**本金 vs. 利息**的圓餅圖。
* **[即時權重更新 (V6.0)]**：
    * 在「分析與設定」頁面，權重拉桿會**即時自動更新**所有物件分數，無需手動儲存。
* **[列表功能升級]**：
    * **物件比較模式 (V6.0)**：在「物件列表」頁面，可勾選 2-4 個物件進行**並排表格比較**。
    * **依狀態過濾 (V7.0)**：您現在可以**篩選**只顯示「待看屋」或「斡旋中」的物件。
    * **依單價排序 (V7.0)**：新增「依單價 (萬/坪) 排序」。
    * **新欄位 (V6.0)**：新增「狀態」和「賞屋日期」欄位。
* **[RWD 行動版優化 (V5.0)]**：
    * 在行動裝置上，左側選單會自動隱藏，並可透過「漢堡選單」叫出。
* **[資料管理]**：
    * **資料本地儲存**：所有資料都會儲存在您的瀏覽器 `localStorage` 中。
    * **備份與還原 (V3.0)**：支援將所有資料匯出為 `JSON` 檔案備份，或從備份檔匯入。

## ⚠️ 關於地圖與 API 功能 (GitHub Pages 限制)

* **地圖 (Map) - [可使用]**：
    * 「地圖總覽」頁面使用的是 **OpenStreetMap (OSM)**。
    * OSM 是免費的，**不需要 API Key**，因此在 GitHub Pages 上**可以正常顯示**。

* **分析 (Analysis) - [僅限本機]**：
    * 「分析通勤」和「自動定位」功能需要串接 Google Maps API。
    * 由於 GitHub Pages 是靜態網站服務，不支援後端代理 (Proxy)，瀏覽器會因「CORS 政策」阻擋對 Google Maps API 的直接請求。
    * 因此，**這些按鈕在 GitHub Pages 線上版中已被停用**（僅顯示提示訊息）。

* **如何在本機測試 API 功能**：
    1.  下載此專案，`npm install`。
    2.  申請 Google Maps API Key 並啟用 **Geocoding API** 和 **Distance Matrix API**。
    3.  修改 `vite.config.ts`，加入 `server.proxy` 設定（詳見 V5.0 說明）。
    4.  在「分析與設定」頁面填入您的 API Key。
    5.  `npm run dev` 啟動，即可在本機使用完整功能。

## 🛠️ 技術棧 (Tech Stack)

* **前端框架**: React 18
* **建置工具**: Vite
* **程式語言**: TypeScript
* **圖表**: Recharts
* **地圖**: Leaflet & React-Leaflet
* **Markdown 渲染**: React-Markdown
* **部署**: GitHub Pages

## 📖 安裝與啟動 (本地開發)

1.  **Clone 專案**
    ```bash
    git clone [https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git](https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git)
    cd YOUR_REPO_NAME
    ```

2.  **安裝/更新 依賴**
    (建議清除快取以確保依賴正確)
    ```bash
    rm -rf node_modules package-lock.json
    npm install
    ```

3.  **啟動本地伺服器**
    ```bash
    npm run dev
    ```
    應用程式將會運行在 `http://localhost:5173`。

## 🚀 部署到 GitHub Pages

1.  **修改 `vite.config.ts`**
    
    打開 `vite.config.ts` 檔案，將 `base` 屬性修改為您的**儲存庫名稱** (Repository Name)。
    
    ```typescript
    // vite.config.ts (V7.0)
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'

    export default defineConfig({
      // 範例：如果您的 repo 網址是 [https://ryan.github.io/property-assistant/](https://ryan.github.io/property-assistant/)
      // 這裡就要填 '/property-assistant/'
      base: '/YOUR_REPO_NAME/', 
      plugins: [react()],
      server: {
        proxy: {} // 保持為空，因為 GH Pages 不支援
      }
    })
    ```

2.  **確認 `package.json`**
    
    確保您的 `package.json` 的 `scripts` 中包含 `predeploy` 和 `deploy`：
    
    ```json
    "scripts": {
      "predeploy": "npm run build",
      "deploy": "gh-pages -d dist"
    },
    ```
3.  **執行部署**
    
    ```bash
    npm run deploy
    ```

4.  **設定 GitHub 儲存庫**
    
    * 前往您的 GitHub 儲存庫頁面 -> **Settings** -> **Pages**。
    * **Source**: 選擇 **Deploy from a branch**。
    * **Branch**: 選擇 `gh-pages` 分支，資料夾 `/(root)`，點擊 **Save**。

等待幾分鐘後，您的網站就會上線！