# 房產決策分析儀 (Property Decision Assistant) v6.0

這是一個使用 React + Vite + TypeScript 建置的個人化房產決策分析工具。

它協助使用者輸入、管理和比較多個待選房產物件，並根據**可自訂權重**（通勤、成本、空間、屋齡）為每個物件計算「綜合推薦分數」，幫助使用者在看房過程中做出更理性的決策。

## 🚀 核心功能 (V6.0)

* **RWD 行動版優化 (V5.0)**：在行動裝置上，左側選單會自動隱藏，並可透過「漢堡選單」叫出，大幅提升手機瀏覽體驗。
* **即時權重更新 (V6.0)**：在「分析與設定」頁面，權重拉桿會**即時自動更新**所有物件分數，無需手動儲存。
* **物件比較模式 (V6.0)**：在「物件列表」頁面，可勾選 2-4 個物件進行**並排表格比較**。
* **新功能頁 (V6.0)**：新增「工具 & 資訊」頁面，包含：
    * 獨立房貸試算
    * 可負擔房價回推
    * 一次性成本估算
    * 購屋檢查清單
    * 常用術語
* **儀表板升級 (V6.0)**：
    * **修正雷達圖**：現在可正確顯示多個物件的「成本」、「通勤」、「空間」、「屋齡」四維度分數。
    * **新增統計**：新增「平均單價」、「平均真實總成本」等 4 個關鍵數據卡。
* **列表功能升級 (V6.0)**：
    * 新增「**狀態**」欄位 (如：待看屋、斡旋中)。
    * 新增「**賞屋日期**」欄位。
    * 卡片上直接顯示「**單價 (萬/坪)**」。
* **可變動的目的地 (V4.0)**：您現在可以自由新增/刪除多個通勤目的地。
* **資料本地儲存**：所有資料都會儲存在您的瀏覽器 `localStorage` 中。
* **備份與還原 (V3.0)**：支援將所有資料匯出為 `JSON` 檔案備份，或從備份檔匯入。

## ⚠️ 關於 API 功能 (GitHub Pages 限制)

「通勤分析」與「自動定位」功能**在 GitHub Pages 線上版中無法使用**。

* **原因**：GitHub Pages 是靜態網站服務，不支援後端代理 (Proxy)。瀏覽器會因「CORS 政策」阻擋對 Google Maps API 的直接請求。
* **本地測試**：您仍可在**本地** ( `npm run dev` ) 啟用此功能。請修改 `vite.config.ts`，加入 `server.proxy` 設定（詳見 V5.0 說明），並在「分析與設定」頁面填入您的 Google API Key。

## 🛠️ 技術棧 (Tech Stack)

* **前端框架**: React 18
* **建置工具**: Vite
* **程式語言**: TypeScript
* **圖表**: Recharts
* **地圖**: Leaflet & React-Leaflet
* **Markdown 渲染**: React-Markdown
* **部署**: GitHub Pages

## 📖 安裝與啟動 (本地開發)

1.  **Clone 專案**
    ```bash
    git clone [https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git](https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git)
    cd YOUR_REPO_NAME
    ```

2.  **安裝/更新 依賴**
    (建議清除快取以確保依賴正確)
    ```bash
    rm -rf node_modules package-lock.json
    npm install
    ```

3.  **啟動本地伺服器**
    ```bash
    npm run dev
    ```
    應用程式將會運行在 `http://localhost:5173`。

## 🚀 部署到 GitHub Pages

1.  **修改 `vite.config.ts`**
    
    打開 `vite.config.ts` 檔案，將 `base` 屬性修改為您的**儲存庫名稱** (Repository Name)。
    
    ```typescript
    // vite.config.ts (V6.0)
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'

    export default defineConfig({
      // 範例：如果您的 repo 網址是 [https://ryan.github.io/property-assistant/](https://ryan.github.io/property-assistant/)
      // 這裡就要填 '/property-assistant/'
      base: '/YOUR_REPO_NAME/', 
      plugins: [react()],
      server: {
        proxy: {} // 保持為空，因為 GH Pages 不支援
      }
    })
    ```

2.  **確認 `package.json`**
    
    確保您的 `package.json` 的 `scripts` 中包含 `predeploy` 和 `deploy`：
    
    ```json
    "scripts": {
      "predeploy": "npm run build",
      "deploy": "gh-pages -d dist"
    },
    ```
3.  **執行部署**
    
    ```bash
    npm run deploy
    ```

4.  **設定 GitHub 儲存庫**
    
    * 前往您的 GitHub 儲存庫頁面 -> **Settings** -> **Pages**。
    * **Source**: 選擇 **Deploy from a branch**。
    * **Branch**: 選擇 `gh-pages` 分支，資料夾 `/(root)`，點擊 **Save**。

等待幾分鐘後，您的網站就會上線！